export interface RegisterProps {
  name: string;
  owner: string;
  duration: number;
  nameHash: string;
  resolverAddr: string;
  isAvailable: boolean;
}

export default function useRegister(props: RegisterProps) {
  return;
}
