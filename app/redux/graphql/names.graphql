query GetNames {
  nameWrappeds {
    name
    id
    domain {
      labelName
      name
    }
  }
}

query GetNamesByName($name: String!) {
  nameWrappeds(where: { name: $name }) {
    name
    owner {
      id
    }
  }
}
